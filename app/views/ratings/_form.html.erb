<%= form_with(model: rating, local: true) do |form| %>
  <% if rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rating.errors.count, "error") %> prohibited this rating from being saved:</h2>

      <ul>
      <% rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <% usuarios = options_from_collection_for_select(@users, 'id', 'username') %>
    <%= form.select :user_id , usuarios%>
  </div>

  <div class="field">
    <%= form.label :movie_id %>
    <% pelis = options_from_collection_for_select(@movies, 'id','title') %>
    <%= form.select :movie_id, pelis %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
